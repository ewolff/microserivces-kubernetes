---
apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceRouter
metadata:
  name: customer
spec:
  routes:
    - match:
        http:
          queryParam:
            - name: "version"
              exact: "v2"
      destination:
        service: catalog
